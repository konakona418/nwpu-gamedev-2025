include "PlayerUpdate.fbs";
include "Events.fbs";
include "Weapon.fbs";

namespace moe.net;

union ReceivedPacketUnion { AllPlayerUpdate, GameEvent }

table ReceivedHeader {
    server_tick: uint64;
    server_ts_ms: uint64;
}

table ReceivedNetMessage {
    header: ReceivedHeader;
    packet: ReceivedPacketUnion;
}

root_type ReceivedNetMessage;
